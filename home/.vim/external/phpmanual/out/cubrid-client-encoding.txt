cubrid_client_encoding -- Returns the current CUBRID connection charset

  string cubrid_client_encoding([resource conn_identifier])~

This function returns the current CUBRID connection charset.

{conn_identifier} The CUBRID connection. If the connection identifier is not
specified, the last link opened by |cubrid_connect| is assumed.

A string that represents the CUBRID connection charset; on success.

FALSE on failure.

|cubrid_client_encoding| example

<?php >
      $con = cubrid_connect("localhost", 33000, "demodb");
      if (!$con)
      {
          die('Could not connect.');
      }
  
      printf("CUBRID current charset: %s\n", cubrid_client_encoding($con));
?>

The above example will output:

  Result:
  CUBRID current charset: iso8859-1

|cubrid_get_charset|

vim:ft=help:
